# Copy to .env.local and fill in. No quotes needed; no trailing slash on URLs.

# Supabase (Settings -> API). URL without trailing slash.
# P0-09：SUPABASE_SERVICE_ROLE_KEY 僅供 server（API routes、scripts）使用，嚴禁設為 NEXT_PUBLIC_*
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Groq (console.groq.com) — chat 主線，必填
GROQ_API_KEY=

# NVIDIA NIM (build.nvidia.com → Get API Key)，選填，chat fallback
NVIDIA_NIM_API_KEY=

# OpenRouter (openrouter.ai/keys). Embedding + chat fallback.
OPENROUTER_API_KEY=
# 若 Pinecone index 為 1024 維，設以下兩項；否則預設 1536（text-embedding-3-small）
# EMBEDDING_MODEL=intfloat/multilingual-e5-large
# EMBEDDING_DIMENSION=1024

# Pinecone: INDEX HOST from console (e.g. https://xxx.svc.region.pinecone.io), no trailing slash
PINECONE_API_URL=
PINECONE_API_KEY=

# Resend：到期提醒 Email（136–140）；P3-65 扣款失敗通知（E88）
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=Cheersin <noreply@yourdomain.com>

# PayPal 訂閱（未設定時 /api/subscription 回 503，前端不重試 — E25）
# PAYPAL_CLIENT_ID=
# PAYPAL_CLIENT_SECRET=
# P0-05：生產環境必填 — Webhook 簽名驗證，未設則偽造 webhook 可改訂閱狀態
# PAYPAL_WEBHOOK_ID=
# P1-12 / P3-73：選填；若設定則 create-subscription 不重複建立 product/plan。plan 對應單一來源，Webhook 與 subscription 皆從此讀取。
# PAYPAL_BASIC_PLAN_ID=
# PAYPAL_PREMIUM_PLAN_ID=
# P1-11：選填，分散式 rate limit（多實例時限流一致）
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# E26：社會認證數字動態化（首頁/定價頁「12,000+ 用戶」）
# NEXT_PUBLIC_SOCIAL_PROOF_COUNT=12000

# E81：限時優惠倒數結束時間（Unix ms）；不設或 0 則定價頁不顯示倒數
# NEXT_PUBLIC_PROMO_END_MS=

# Optional
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Cheersin
# 遊戲房間：僅在設為 true 時使用 in-memory mock（預設連真實 Supabase）
# NEXT_PUBLIC_GAMES_ROOM_MOCK=true

# P3-51：Chat 模型順序（選填）。CHAT_PRIMARY=groq|nim|openrouter；CHAT_FALLBACK_ORDER=groq,nim,openrouter
# CHAT_PRIMARY=groq
# CHAT_FALLBACK_ORDER=groq,nim,openrouter

# P3-47：CORS 白名單（選填）。僅在需跨域呼叫 /api 時設定單一 origin 或逗號分隔
# CORS_ALLOWED_ORIGINS=https://admin.example.com

# P3-35：Analytics 額外持久化事件（選填，逗號分隔）
# ANALYTICS_PERSIST_EXTRA=custom_event_1,custom_event_2
